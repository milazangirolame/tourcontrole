<% if is_admin? %>
  <div class="btn-group">
    <%= link_to 'Editar', edit_activity_path(activity), class: 'btn btn-primary btn-md'  %>
    <%= link_to 'Excluir', activity_path(activity), method: :delete, data: {confirm: "Esta ação é definitiva \n Tem certeza que quer excluir o tour? \n #{activity.name}  "}, class: 'btn btn-danger btn-md'  %>
  </div>
<% end %>
